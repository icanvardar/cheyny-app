import { DefaultTheme } from "@react-navigation/native";

// Default theme that is used for theme provider
const THEME = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: "#E2BC65",
    background: "#131A2D",
    backgroundSecondary: "#18223C",
    card: "#18223C",
    text: "#FFFFFF",
    border: "#424857",
  },
};

export default THEME;

// background *
// border *
// card *
// notification *
// primary *
// text *
